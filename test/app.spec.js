/*describe('mainFunction', () => {

    it('function to return result', () => {
      expect(mainFunction(param)).toBe('result');
    });
});

describe('getMDFiles', () => {

    it('should return invalid file error', () => {
      expect(getMDFiles()).toBe('result');
    });
});
*/